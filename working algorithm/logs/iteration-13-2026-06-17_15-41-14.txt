Task #247 is **blocked by the environment**, not by the code. Here's the situation.

## What I found

Task #247 (the final RBAC item, 12/12) is the Playwright e2e for role-based access on Nomenclatures & Compositions. The code deliverable was already written (uncommitted) by a prior iteration, and I reviewed it as essentially complete:

- `e2e/tests/access-control/rbac-nomenclatures-compositions.spec.ts` ΓÇö three roles (noperm / readonly / admin) selected via per-project `test.skip`
- `rbac-seed.helper.ts`, `global-setup.ts` pre-seeding, `playwright.config.ts` `noperm` project, and the two page objects
- I verified every `data-testid` the spec relies on actually exists in `src/` (nav items, nomenclature/composition buttons)

## The blocker (shared infra, outside this task's code)

Running `npm run e2e -- e2e/tests/access-control` fails at **global-setup**: the admin MSAL login times out waiting for the app JWT. Root cause:

`osdm-user-service-1` throws on every auth request:
```
System.ArgumentException: IDX10703: Cannot create a SymmetricSecurityKey, key length is zero  (Program.cs:97)
```
`Jwt__Secret` is **empty** (length 0) on user-service *and* rail-run-service, while `Azure__KeyVault__VaultUrl` is set ΓÇö the JWT signing secret is meant to come from Azure KeyVault and is resolving empty. So no service can issue/validate the application JWT, and every e2e login hangs. (The prior agent's leftover `diag-login.mjs` suggests they hit the same wall ΓÇö which is why #247 is still uncommitted and `passes:false`.)

Fixing this means restoring a non-empty **shared** JWT secret across the OSDM services (fix KeyVault connectivity, or inject the same secret and recreate the containers). That's a disruptive change to your running 16-service stack and could be guessing at secrets ΓÇö so I'm **not** doing it autonomously.

## Two regressions to fix before this is committable (once unblocked)

1. `global-setup.ts`'s `preSeed: 'readonly'` wipes the **shared** readonly account down to only NOMENCLATURES+COMPOSITION ΓÇö the existing `readonly`-project suite (roles/users/tariffing/iasutd/groups/messaging view specs) needs a *broad* readonly account and would break. Better: seed ReadOnly on **all** resources.
2. `playwright.config.ts` adds a global `noperm` project with no `testMatch`, so every existing spec runs under a zero-permission session and fails. Better: scope `noperm`'s `testMatch` to `**/access-control/**`.
3. `e2e/diag-login.mjs` is a throwaway debug script with hardcoded creds ΓÇö should not be committed.

I left `tasks.json` at `passes: false` and logged all of the above to `ralph/activity.md`.

**How do you want to proceed?** Options: (a) you/I restore the JWT secret + restart the auth services so I can verify e2e green, then I apply the two regression fixes and commit; or (b) I apply the regression-hardening edits now (code-only, safe) but leave it uncommitted/unverified until the env is fixed.

