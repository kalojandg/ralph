Task #220 done ΓÇö no tasks remain with `passes: false`.

**Summary:** Surfaced the source composition's train number in the `composition_cloned` audit event so the history header reads `(╨╕╨╖╤é╨╛╤ç╨╜╨╕╨║ ╨æ╨Æ 2601-ΓÇª)` instead of the raw `(╨╕╨╖╤é╨╛╤ç╨╜╨╕╨║ #3)`.
- **Backend** (`CloneCompositionForPeriod.cs`): added `sourceTrainNumber`/`sourceStartDate` to the audit `WithDetails` (kept `sourceId` for back-compat). REDΓåÆGREEN audit test passes; container rebuilt + recreated.
- **Frontend** (`compositionHistory.utils.ts` + bg/en locales): `composition_cloned` now renders `sourceTrain` from `sourceTrainNumber`, falling back to `#<sourceId>` for older events. 22/22 tests, type-check, eslint all clean.
- Committed to both repos (OSDM-Src `82922ad23`, Admin-App `52cf6dd4`); tasks.json #220 ΓåÆ `passes: true`; activity.md logged.

Note: I verified the rendering via unit tests rather than a live clone-flow browser walkthrough (that needs seed data / a manual clone) ΓÇö the label logic is deterministic and fully covered on both sides.

```xml
<task-complete>
  <task-id>220</task-id>
  <tests>PASSED</tests>
  <visual>N/A</visual>
  <committed>YES</committed>
</task-complete>
```
```xml
<promise>COMPLETE</promise>
<total-tasks>204</total-tasks>
<all-passed>true</all-passed>
```

All 204 tasks now pass. This iteration is over ΓÇö stopping here.

