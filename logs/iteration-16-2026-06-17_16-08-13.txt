Task #247 is **blocked by an environment issue, not a code defect** ΓÇö and this is the 4th iteration to hit it. Leaving `passes: false` and stopping the loop.

## Iteration Summary

**Task:** #247 ΓÇö [RBAC 12/12][E2E] Playwright real FEΓåÆBEΓåÆDB role-based access for Nomenclatures & Compositions

**Deliverable code:** Γ£à Complete and verified present (from prior iterations 236ΓÇô246):
- `e2e/tests/access-control/rbac-nomenclatures-compositions.spec.ts` (noperm / readonly / admin scenarios)
- `e2e/helpers/rbac-seed.helper.ts`, `e2e/global-setup.ts` (3-account seeding), `playwright.config.ts` (3 projects)
- Page objects `nomenclatures.page.ts` / `compositions.page.ts`
- Source `data-testid`s and menu/route gating in `MainLayout.tsx`, `NomenclatureTable.tsx`, `CompositionsListPage.tsx`, `CompositionList.tsx` ΓÇö all confirmed.

**Blocker (environment):** `npm run e2e -- e2e/tests/access-control` failed in **global-setup MSAL login**, not in the tests. The user-service container crashes on every auth request:
```
IDX10703: Cannot create a SymmetricSecurityKey, key length is zero   (UserService.API/Program.cs:90/105)
```
`Jwt__Secret` is **0 bytes** in `osdm-user-service-1` (host `.env` has no `JWT_SECRET`; the value is meant to come from Key Vault, and `DefaultAzureCredential` fails inside the container). With user-service unable to issue/validate JWTs, the FE token-exchange never completes, login stays on `/login` ("An unexpected error occurred"), and no E2E can authenticate. The container restarted ~40 min ago and came back without the secret.

**Why I did not mark it passed or "fix" it:** Per team convention, merge-readiness requires the real FEΓåÆBEΓåÆDB e2e to run green ΓÇö impossible while auth is down. I did not invent a JWT secret and recreate shared backend containers: that's high-blast-radius shared-infra surgery requiring the real Key Vault secret, and three prior iterations already deferred it to humans.

**Action needed (human / shared-infra):** Restore the user-service JWT secret ΓÇö either fix Key Vault access for the containers, or authorize injecting one shared `Jwt__Secret` across the OSDM services and `docker compose up -d --force-recreate`, then re-run `npm run e2e -- e2e/tests/access-control` to verify and commit.

**Status:** `<status>BLOCKED</status>` ΓÇö `passes` stays `false`. Recommend **pausing the Ralph loop on #247** until JWT auth is restored; further iterations will keep re-blocking with zero code progress.

