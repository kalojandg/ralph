The blocker is unchanged ΓÇö `Jwt__Secret=` is still empty and `osdm-user-service-1` is still unhealthy, identical to all 8 prior verifications. I'm stopping rather than running a 9th no-progress iteration.

## Task #247 ΓÇö the only remaining task ΓÇö is blocked on shared infrastructure, not code

**The code deliverable is complete and verified static-green** (uncommitted in the Admin-App working tree):
- `e2e/tests/access-control/rbac-nomenclatures-compositions.spec.ts` (noperm / readonly / canedit scenarios)
- `e2e/helpers/rbac-seed.helper.ts` (Mongo UserService role seeding)
- modified `e2e/{global-setup.ts, playwright.config.ts, page-objects/nomenclatures.page.ts, page-objects/compositions.page.ts}`
- eslint clean, `type-check` clean, all referenced `data-testid`s confirmed present in `src`.

**Why it can't be verified (root cause, confirmed again just now):**
`osdm-user-service-1` boots with an empty `Jwt__Secret` because its KeyVault `DefaultAzureCredential` fails inside the container. Every auth request throws `IDX10703: Cannot create a SymmetricSecurityKey, key length is zero`. The service issues no JWTs ΓåÆ the Playwright global-setup MSAL login never completes ΓåÆ the access-control spec aborts before any test runs. This is a real-FEΓåÆBEΓåÆDB e2e task, so it genuinely cannot pass without working auth.

**I did not** mark `passes: true` (unverifiable) and **did not** perform the one unblocking action on my own, because it's high-blast-radius shared-infra surgery that needs your authorization.

## What I need from you ΓÇö pick one

1. **Fix KeyVault access** so the containers can fetch the real secret (e.g. an `az login` the containers can reach, or managed-identity / service-principal env), then recreate the OSDM services so all three load the same `Jwt:Secret`. ΓÇö *Cleanest, but I can't do this for you.*

2. **Authorize injecting one shared `Jwt__Secret`** into all OSDM services in `docker-compose.yml` and `docker compose up -d --force-recreate`. ΓÜá∩╕Å Caveat from the prior logs: the rail-run/nomenclature services have been up 2 days and still hold a *valid* in-memory secret from an earlier KeyVault-OK start ΓÇö recreating them brings the whole stack up with the new explicit secret, which is fine only if **all** services get the same value. If you authorize this, tell me and I'll do it.

Once auth is restored, the rest is mechanical: `cd Admin-App && npm run e2e -- e2e/tests/access-control` ΓåÆ if green, commit `feat(compositions): [RBAC 12/12][E2E] ...` and flip #247 to `passes: true`, completing the RBAC feature (236ΓÇô247).

I'm **not** rescheduling another loop iteration ΓÇö re-running would just reproduce this same block. How would you like to proceed?

