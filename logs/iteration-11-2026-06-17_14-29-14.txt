Task #246 complete. i18n was already satisfied (`permissions.accessDenied` present in both locales, no new strings introduced by 240-245), and I added stable `data-testid` hooks to all controls task #247's e2e will target, then verified and committed.

```xml
<task-complete>
  <task-id>246</task-id>
  <tests>PASSED</tests>
  <visual>N/A</visual>
  <committed>YES</committed>
</task-complete>
```

There is still one task with `passes: false` (#247 ΓÇö the Playwright E2E for RBAC).

```xml
<status>CONTINUE</status>
<next-task>247</next-task>
```

## Iteration Summary

**Task Worked On:** #246 ΓÇö [RBAC 11/12][FE] i18n + a11y sweep

**TDD Phase Completed:** N/A (setup task, `tddWorkflow: false`)

**What changed:**
- i18n: confirmed `permissions.accessDenied` already exists in both `bg.json` and `en.json`; tasks 240-245 added no new UI text, so nothing to add.
- data-testid hooks added: `nomenclature-add-button`, `nomenclature-edit-button-${id}`, `nomenclature-delete-button-${id}`, `composition-create-button`, `composition-edit-button-${id}`, `composition-delete-button-${id}`, `composition-save-button`, and nav items `nav-nomenclatures`, `nav-compositions`, `nav-compositions-assembly/-wagons/-history`.

**Verification:** type-check green; eslint 0 errors (14 pre-existing warnings untouched); 964 tests pass. 11 failures in `CreateCompositionModal.test.tsx` are pre-existing (verified identical on unmodified HEAD via `git stash`) and unrelated to this change.

**Next:** #247 ΓÇö Playwright E2E covering real FEΓåÆBEΓåÆDB role-based access (will consume these testids).

