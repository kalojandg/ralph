Task #231 done. Added `POST /api/currencies/import` to `CurrencyController` following the export action's pattern: a new `NomenclatureImportRequest` multipart DTO, format inferred from the file extension when blank and validated against `ExportFormats.Supported`, dispatches the generic `ImportNomenclatureCommand` (TypeKey `"currency"`), returns `Result<ImportResult>`.

One deviation worth flagging for the next iteration (task 232): the task said authorize with `AccessLevel.ReadWrite`, but that value doesn't exist ΓÇö the enum is `NoAccess/ReadOnly/CanEdit`. I used `CanEdit` (the codebase's write level). The remaining 29 import endpoints should use `CanEdit` too.

Verification: 8/8 CurrencyController tests pass, full API.Tests 195/195 green, `gitnexus detect-changes` shows only CurrencyController touched (0 affected processes, low risk).

```xml
<task-complete>
  <task-id>231</task-id>
  <tests>PASSED</tests>
  <visual>N/A</visual>
  <committed>YES</committed>
</task-complete>
```

```xml
<status>CONTINUE</status>
<next-task>232</next-task>
```

Note: a post-commit hook reports the GitNexus index is now stale ΓÇö the next iteration's RECON can refresh it with `npx gitnexus analyze` if cross-file impact is needed. Stopping here per the one-task-per-iteration rule.

