Task #247 is blocked for the 9th consecutive iteration by the same environment issue ΓÇö I am stopping the loop rather than spinning again.

**Status: Γ¢ö BLOCKED ΓÇö not marked `passes: true`.**

**Root cause (unchanged across 9 verifications):** `osdm-user-service-1` has an empty `Jwt__Secret` because KeyVault's `DefaultAzureCredential` fails inside the container. Every auth request throws `IDX10703: SymmetricSecurityKey, key length is zero`, so the user-service issues no JWTs. The Playwright e2e suite aborts in `global-setup` (MSAL login can't complete) before any access-control spec runs ΓÇö so the test is unverifiable.

**The code deliverable is complete and hardened** (uncommitted, verified eslint/type-check clean in prior iterations):
- `e2e/tests/access-control/rbac-nomenclatures-compositions.spec.ts`
- `e2e/helpers/rbac-seed.helper.ts`
- modified `e2e/{global-setup.ts, playwright.config.ts, page-objects/{nomenclatures,compositions}.page.ts}`

**Why I didn't proceed autonomously:** the only unblocking action is shared-infra surgery ΓÇö injecting one shared `Jwt__Secret` across all OSDM services and `docker compose up -d --force-recreate`, or restoring KeyVault access. That has high blast radius (the 2-days-up nomenclature/rail-run services still hold a valid in-memory secret; recreating them brings the whole stack up secret-less and breaks cross-service JWT validation). This needs your authorization.

**What I need from you (pick one):**
1. Authorize me to inject a shared `Jwt__Secret` into the OSDM services in docker-compose and recreate them, or
2. Restore KeyVault access (`az login` reachable in-container / managed identity / service-principal env), then I'll run the suite, and if green, commit and set `#247 passes: true`.

<status>BLOCKED</status>

Recommend keeping the Ralph loop **stopped** on #247 until JWT auth is restored ΓÇö further iterations will keep re-blocking with zero code progress.

