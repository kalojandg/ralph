Task #217 complete ΓÇö one task, then stop per the Ralph Wiggum algorithm.

**Task #217 ΓÇö Make the WAGON TYPE searchable (AuditService)**

- **RECON:** `AddDetailsScopeIds` read only top-level DetailsJson keys; the wagon series lives inside the bulk `old[]/new[]` snapshots (added in #212), so it wasn't searchable. Impact on `AcceptAuditEventAsync`: MEDIUM, 7 upstream (all tests).
- **RED:** Added two failing tests ΓÇö bulk `old[]/new[]` snapshots surface `wagonTypeName`/`wagonTypeId`/`placardNumber` into `SearchText`, plus a top-level wagon-type case. Both failed for the right reason (`15-63` missing).
- **GREEN:** Added `wagonTypeId`/`wagonTypeName` to `ScopeIdProperties` and a new `AddSnapshotArrayScopeIds` that iterates the `old`/`new` arrays indexing each element's type name/id/placard; refactored scalar extraction into `AddScalar`. Full project: 414 pass, 1 pre-existing unrelated failure (hard-coded event-type count, `EventTypes.cs` untouched).
- **DONE:** Rebuilt + force-recreated `audit-service`; idempotent OPENJSON backfill updated 5 existing rows (SearchText now contains series `15-63`/`19-40`/`20-44`); `gitnexus detect-changes` low risk; committed `522236239`; `tasks.json` #217 ΓåÆ `passes: true`, activity.md updated.

Files: `AuditLoggingService.cs`, `AuditLoggingServiceTests.cs`.

<task-complete>217</task-complete>
<status>CONTINUE</status>

Remaining incomplete task: **#218** (frontend ΓÇö relabel WagonHistoryPage to type/series, now searchable end-to-end via #217). It will be picked up by the next iteration with a clean context.

Note: a post-commit hook flagged the GitNexus index as stale (`npx gitnexus analyze` to refresh) ΓÇö left for the next iteration since it doesn't affect this task's result.

