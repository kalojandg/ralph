Task #203 complete. The template's keys diverged from what the #201 components actually consume, so I matched the components (source of truth) and fixed interpolation to the store's `{{double-brace}}` syntax.

## Iteration Summary

**Task Worked On:** #203 ΓÇö Frontend i18n: add `compositions.history.*` keys to bg.json and en.json

**TDD Phase Completed:** N/A (locale-only setup task, no `tddWorkflow`)

**Tests Status:**
- Unit/component: PASSED (46/46 ΓÇö i18n parity, history page, summary-builder util)
- E2E: N/A (locale-only, no UI-flow code touched)
- Type-check: PASSED

**Key decisions:**
- Matched the **actual component keys** (`filters.*` plural, `states.empty/error`, `view.table/timeline`, `allWagons/allEventTypes/allOperations/allLevels`, `status.failed`) instead of the diverged template ┬º5/┬º6.
- Used `{{double-brace}}` interpolation (the store's syntax), not the template's single-brace.
- Also added the two wiring keys #202 references: `compositions.editor.header.history` and `navigation.compositionsMenu.history`.
- No hardcoded Cyrillic remained in the new components.

**Verification:** bgΓçäen parity true; all 46 referenced keys resolve in both files; committed `bg.json` + `en.json` only (CLAUDE.md left unstaged as the GitNexus artifact, per prior tasks).

```xml
<task-complete>
  <task-id>203</task-id>
  <tests>PASSED</tests>
  <visual>N/A</visual>
  <committed>YES</committed>
</task-complete>
```

```xml
<status>CONTINUE</status>
<next-task>204</next-task>
```

Stopping now ΓÇö task #204 (e2e) is the next `passes: false` and will be handled by a fresh agent in the next iteration.

