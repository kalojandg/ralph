Task #221 complete. The TripPicker results table now shows each trip's validity date range, hides fully-past trips, and sorts by earliest validity date.

- **RED:** added a `Validity dates` describe block to `TripPicker.test.tsx` (range rendering, single-date, past-hiding, ascending sort); gave the shared fixtures `activeDates`. 4 new tests failed for the right reason.
- **GREEN:** in `TripPicker.tsx` derived `visibleResults` via `useMemo` (dayjs parse ΓåÆ start/end, filter past, sort by start), added a `Validity` column + `formatValidity`; added `columns.validity` to both `bg.json` and `en.json`.
- **DONE:** 23/23 tests pass, `type-check` clean, eslint clean (0 errors/0 warnings on changed files), `gitnexus detect-changes` = low risk / scope contained to TripPicker.

`tasks.json` #221 set to `passes: true`, `activity.md` logged, committed as `f5b314c7`. (CLAUDE.md had a pre-existing unrelated modification ΓÇö left unstaged.)

```xml
<task-complete>
  <task-id>221</task-id>
  <tests>PASSED</tests>
  <visual>N/A</visual>
  <committed>YES</committed>
</task-complete>
```

All tasks in `tasks.json` are now `passes: true` ΓÇö #221 was the last remaining one.

```xml
<promise>COMPLETE</promise>
<total-tasks>221</total-tasks>
<all-passed>true</all-passed>
```

Iteration over ΓÇö stopping here.

