Diagnosis logged. Task #263 left `passes: false` and uncommitted (per the e2e gate). No infra was mutated.

## Iteration Summary

**Task Worked On:** #263 ΓÇö [WAGON-OCC 8/8][E2E] Playwright real FEΓåÆBEΓåÆDB COMP-04 capstone (the last remaining `passes:false` task)

**Outcome:** Γ¢ö Blocked by environment ΓÇö not a code defect.

**What I verified:**
- Delivered code (from the prior iteration) is intact and green: `type-check` clean, `eslint` on changed files = 0 errors (14 pre-existing warnings).
- Live auth probe: the stored e2e JWT is valid (unexpired to 2026-06-30), but `GET /api/v1/reports/exports/SYS-OPS-COMP-04` ΓåÆ **HTTP 401**. Backend can't validate it because `Jwt:Secret` is null.

**Sharper root cause (corrects the prior "az login" note):**
- The host **is** already logged into Azure and **can** read `Jwt--Secret` from `bdz-test-kv`. `az login` was never the missing step.
- The actual blocker: `docker-compose.yml` mounts `${USERPROFILE}/.azure:/root/.azure:ro` (**read-only**). The container's `AzureCliCredential` can't write `az.sess` ΓåÆ KeyVault unreachable ΓåÆ null secret. A plain `--force-recreate` (the old remediation) re-hits the same `:ro` mount and would **not** fix it.

**Tests:** type-check PASSED ┬╖ lint PASSED (0 errors) ┬╖ e2e NOT RUN (auth 401)
**passes:** false ┬╖ **Committed:** NO

**To finish (needs your go-ahead ΓÇö both are shared-infra changes):**
- **Option A (recommended, reversible):** `az keyvault secret show --vault-name bdz-test-kv --name Jwt--Secret --query value -o tsv`, then recreate only `rail-run-service` + `reporting-service` with that value injected as env `Jwt__Secret` (transient override), run the e2e.
- **Option B (permanent):** flip the `.azure` mount to `:rw` for those two services and recreate.

Then `npm run e2e -- e2e/tests/compositions/wagon-occupancy-report.spec.ts --project=admin` ΓåÆ flip #263 to `passes:true`, log, and commit.

```xml
<task-complete>
  <task-id>263</task-id>
  <tests>BLOCKED</tests>
  <visual>N/A</visual>
  <committed>NO</committed>
</task-complete>
<status>BLOCKED</status>
<reason>e2e auth 401: rail-run/reporting Jwt:Secret null because the :ro .azure mount blocks the container's KeyVault access. Fix requires recreating shared dev containers ΓÇö needs user confirmation.</reason>
```

Tell me which option you want (A or B) and I'll apply it and drive #263 to green; otherwise it stays `passes:false` for you to handle manually.

