Task #247 is blocked ΓÇö for the **7th** iteration in a row ΓÇö on an environment issue, not a code issue. Stopping the loop here rather than burning another no-progress cycle.

## What's wrong

`osdm-user-service-1` can't issue JWTs:
- `Jwt__Secret=` is **empty** in the container (`docker inspect`)
- It's meant to load from Azure KeyVault (`bdz-test-kv.vault.azure.net`), but `DefaultAzureCredential` fails in-container ΓåÆ empty fallback
- Every auth request throws `IDX10703: Cannot create a SymmetricSecurityKey, key length is zero`
- So the Playwright global-setup MSAL login never gets an app token ΓåÆ the access-control spec **cannot run**

The user-service restarted ~56 min ago and came up secret-less; the other 15 services are still Up 2 days holding a valid in-memory secret from when KeyVault last worked.

## The code is done

The deliverable for #247 is complete and eslint-clean, just uncommitted and unverified at runtime:
- `e2e/tests/access-control/rbac-nomenclatures-compositions.spec.ts`
- `e2e/helpers/rbac-seed.helper.ts`
- modified `e2e/{global-setup.ts, playwright.config.ts, page-objects/nomenclatures.page.ts, page-objects/compositions.page.ts}`

I did **not** mark `passes: true` (can't verify) and did **not** commit.

## I need your call ΓÇö one of these unblocks it

1. **Fix KeyVault access** (you / infra): make `az login` reachable by the containers (or supply a service principal / managed identity), recreate the OSDM services so all reload the **same** secret.
2. **Authorize me to inject a shared secret**: I add one explicit `Jwt__Secret` to all OSDM services in `docker-compose.yml` and `docker compose up -d --force-recreate`. This is high blast radius (recreates ~16 services), so I won't do it without your go-ahead.

Once auth works: `cd Admin-App && npm run e2e -- e2e/tests/access-control` ΓåÆ if green I'll commit and set #247 `passes: true`.

I'm **pausing the loop** rather than re-firing ΓÇö 6 prior iterations reached the same conclusion and looping again changes nothing until the JWT backend is restored. Tell me which option you want (or say "go" for option 2) and I'll proceed.

<status>BLOCKED</status>
<next-task>247 (env-blocked, awaiting human/infra action)</next-task>

