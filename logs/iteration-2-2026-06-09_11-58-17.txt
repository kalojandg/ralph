I'll hold off on writing code until we resolve the entry-point conflict, since it changes both the implementation and the RED test (215.2).

Here's the situation in brief:

- **The core page is straightforward and I can build it correctly now:** `WagonHistoryPage` reads a physical wagon number from `?wagonNumber=` (and/or an input field), calls the existing `useCompositionsHistory({ searchText: wagonNumber, ... })`, and renders the chronology reusing `TimelineView` + `CompositionHistoryDiff` + `buildCompositionHistoryHeader`, with loading/empty/error states and the deferred-operations info note. This matches UC-COMP-12 and task #214 (which indexed `UicNumber` into the audit `searchText`).

- **The blocker is only step 215.2/215.3's per-row "╨ÿ╤ü╤é╨╛╤Ç╨╕╤Å" action on "╨ú╨┐╤Ç╨░╨▓╨╗╨╡╨╜╨╕╨╡ ╨╜╨░ ╨▓╨░╨│╨╛╨╜╨╕".** That page (`WagonsPage`) lists wagon **types** ΓÇö `WagonTypeDto` has just `wagonTypeId` + `seriesName`, no physical wagon number. So there's literally no `wagonNumber` to pass from those rows. The physical number lives only on composition carriages (`DraftWagon.wagonNumber` in the composition editor).

My recommendation: build the page with a wagon-number **input + `?wagonNumber=`** as the entry point and adjust the 215.2 test accordingly, since that's correct and self-sufficient. Optionally, the genuinely-correct per-row action belongs on the **composition editor's wagon rows** (where a real `wagonNumber` exists), not the wagon-types list.

How would you like the page to be reachable? (Input-only / wire from composition editor / force it onto the wagon-types list by series / you want to look first.)

