# Ralph Wiggum Алгоритъм за Claude Code

Този скрипт итеративно извиква Claude CLI докато не получи сигнал за завършване.

## 🪟 Windows Решения

### 🚀 СУПЕР БЪРЗО: Двойно кликване (ralph-quick.bat)
**Просто кликни два пъти на файла!**
- ✅ Няма нужда да пишеш команди
- ✅ Стартира автоматично с 10 итерации
- ✅ Показва резултата и изчаква (не се затваря веднага)

### ⭐ ПРЕПОРЪЧИТЕЛНО: "Изпълним" файл (ralph.exe.bat)
**Двойно кликване:** Ще те попита за брой итерации
**От CMD:**
```cmd
ralph.exe.bat 10
```
- ✅ Интерактивен режим при двойно кликване
- ✅ Command-line режим с параметър
- ✅ Автоматично извиква PowerShell скрипта
- ✅ Няма нужда от специални права

### Опция 1: Batch файл (ralph.bat)
```cmd
ralph.bat 10
```
- ✅ Работи директно от CMD
- ⚠️ По-ограничена функционалност

### Опция 2: PowerShell (ralph.ps1)
```powershell
powershell -ExecutionPolicy Bypass -File ralph.ps1 10
```
или просто:
```powershell
.\ralph.ps1 10
```
- ✅ По-мощен и надежден
- ✅ По-близък до оригиналния bash скрипт
- ⚠️ Може да изисква разрешение за ExecutionPolicy

### Опция 3: Git Bash Wrapper (ralph-bash.bat)
```cmd
ralph-bash.bat 10
```
- ✅ Използва оригиналния ralph.sh
- ⚠️ Изисква Git for Windows

### Опция 4: Компилиране в истински .EXE
```powershell
.\compile-to-exe.ps1
```
Това ще създаде `ralph.exe` който можеш да стартираш така:
```cmd
ralph.exe 10
```
- ✅ Истински изпълним файл
- ✅ Не се нуждае от PowerShell
- ⚠️ Изисква инсталиране на ps2exe модул

## 📝 Изисквания

1. **Claude CLI** трябва да е инсталиран и достъпен в PATH
2. **PROMPT.md** файл в същата директория
3. За PowerShell: може да е нужно да разрешиш изпълнение на скриптове:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

## 🎯 Как работи

1. Скриптът приема брой итерации като аргумент
2. За всяка итерация:
   - Прочита PROMPT.md
   - Извиква `claude` с този prompt
   - Проверява за `<promise>COMPLETE</promise>` в резултата
   - Ако го намери - спира успешно
   - Ако не - продължава
3. След max итерации - излиза с грешка

## 💡 Примери

### 🖱️ С двойно кликване (най-лесно):
1. **ralph-quick.bat** - Кликни два пъти → Веднага стартира с 10 итерации
2. **ralph.exe.bat** - Кликни два пъти → Ще те попита колко итерации искаш
3. **ralph.bat** - Същото като ralph.exe.bat

### ⌨️ От Command Line:
```cmd
REM 5 итерации
ralph.exe.bat 5

REM 20 итерации
ralph.bat 20

REM С PowerShell директно
powershell -ExecutionPolicy Bypass -File ralph.ps1 10

REM Използване на оригиналния bash скрипт
ralph-bash.bat 15
```

## 🔧 Troubleshooting

**Кликнах на .bat файла и нищо не се случи / прозорецът веднага изчезна:**
- ✅ **ПОПРАВЕНО!** Всички .bat файлове вече:
  - Или питат за параметри (ralph.exe.bat, ralph.bat)
  - Или стартират с defaults (ralph-quick.bat)
  - Изчакват с `pause` в края за да видиш резултата
- Пробвай **ralph-quick.bat** - това е най-лесното решение!

**PowerShell казва "cannot be loaded because running scripts is disabled":**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
**Или използвай `ralph.exe.bat` - той минава това ограничение!**

**.ps1 файл се отваря като текст вместо да се изпълнява:**
- Това е нормално поведение под Windows (security feature)
- ✅ Използвай `ralph-quick.bat` или `ralph.exe.bat` за лесно изпълнение
- Или компилирай в .exe с `compile-to-exe.ps1`
- Или стартирай от CMD: `powershell -File ralph.ps1 10`

**Git Bash не се намира:**
- Инсталирай Git for Windows от https://git-scm.com/download/win
- Или използвай ralph.bat или ralph.exe.bat

**Claude не се намира:**
- Увери се че Claude CLI е инсталиран
- Провери дали е в PATH: `where claude` или `Get-Command claude`

## 📝 Windows vs Linux различия

| Linux/Mac | Windows Еквивалент |
|-----------|-------------------|
| `chmod +x ralph.sh` | Не е нужно! Използвай `.bat` или `.exe` |
| `./ralph.sh 10` | `ralph.exe.bat 10` или `ralph.bat 10` |
| Bash скриптове | PowerShell скриптове (`.ps1`) или Batch (`.bat`) |
