# Harness iteration logs — създават се на всяка итерация, не влизат в репото
logs/

# Swarm runtime state — claims са per-session locks, results са агентски отчети,
# retry са failure контексти за следващия опит (изтриват се при успешен merge)
claims/
results/
retry/
